data_source analytics:
  type: duckdb
  # Using environment variables for the database path to ensure portability
  # across local, Docker, and CI/CD environments.
  path: ${DUCKDB_PATH:-analytics.duckdb}
